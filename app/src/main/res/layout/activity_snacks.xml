<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SnacksActivity"
    android:padding="20dp">

    <Button
        android:id="@+id/snacks_button_my_order"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/my_order"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:paddingVertical="10dp"
        android:paddingHorizontal="24dp"
        android:drawableTop="@drawable/ic_baseline_shopping_basket"
        android:background="@drawable/button_background_outline_only"
        android:textColor="@color/dark_gray"
        android:onClick="redirectToMyOrderActivity"
        />
    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/snacks"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/snacks_button_my_order"
        app:layout_constraintTop_toTopOf="@id/snacks_button_my_order"
        app:layout_constraintBottom_toBottomOf="@id/snacks_button_my_order"
        android:textSize="24sp"
        android:textAllCaps="true"
        android:textStyle="bold"
        android:layout_margin="5dp"
        android:textColor="@color/dark_orange"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/snacks_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/snacks_button_my_order"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginVertical="20dp"
        android:scrollbars="vertical"/>

</androidx.constraintlayout.widget.ConstraintLayout>